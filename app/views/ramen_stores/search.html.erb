<% content_for(:title, '見つける') %>
<!-- 検索フォーム -->
<div class="row">
  <div class="col-lg-10 offset-lg-1">
    <%= render 'search_form' %>
  </div>
</div>
<!-- 店舗一覧 -->
<div class="pt-3">
  <div class="row">
    <div class="col-12">
      <div class="row">
        <% if @ramen_stores.present? %>
        <%= render @ramen_stores %>
      </div>
        <%= paginate @ramen_stores %>
        <% else %>
          店舗がまだありません
        <% end %>
      </div>
    </div>
  </div>
</div>
