<% content_for(:title, '店舗一覧') %>
<!-- 店舗一覧 -->
<div class="pt-3">
  <div class="row">
    <div class="col-12">
      <div class="row">
        <% if @ramen_stores.present? %>
          <%= render @ramen_stores %>
        <% else %>
          <%= '投稿がまだありません' %>
        <% end %>
      </div>
    </div>
  </div>
</div>

<%= paginate @ramen_stores %>